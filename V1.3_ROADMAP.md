# 看见地球 v1.3 开发路线图

> 从 v1.2 "内容+SEO+性能完整" 推进到 v1.3 "视觉真实 + 体验增强 + 字段一致"。

## 目标

- **视觉层**：每座城市的 4 张不同时段图都是真实场景（不再 focus 偏移同图）
- **一致性层**：所有中文字段后缀统一 `Zh`（`cn` 全清）
- **体验层**：键盘 power user 流 / PWA 离线 / Lighthouse 性能门禁

## 优先级总览

| 优先级 | 任务 | 工作量 | 价值 |
|---|---|---|---|
| **P0** | PR #11 · 多时段图（每城 2-4 张真实图） | 2-3 天 | 中（视觉真实度） |
| **P0** | PR #12 · 字段名 zh 化（LiveEvent + HistoricalMoment + 引用层） | 0.5 天 | 低（一致性） |
| P1 | PR #13 · 全局键盘快捷键 | 0.5 天 | 中（power user） |
| P1 | PR #14 · PWA + 离线缓存 | 1 天 | 中（留存） |
| P1 | PR #15 · Lighthouse 性能门禁 | 0.5 天 | 中（性能） |

**总计**：约 5-6 天可达 v1.3 上线门槛。

---

## Phase 1 · 视觉与一致性（v1.3 P0）

### PR #11 · 多时段图（v1.2 Q4=b 升级）

**目标**：每座城市的 4 张时段图（morning / afternoon / evening / night）改为真实场景。

**实现方式**：
- **A. Unsplash 多图**：每城找 4 张不同时段的 Unsplash 图（搜索关键词：city + morning/afternoon/evening/night），URL 直接替换
- **B. 自拍/委托拍摄**：编辑自己拍或委托摄影师
- **C. 混合**：6 城精选（板块 2 出现）做 A+B 兼顾（2 张 Unsplash + 2 张实拍），其余 6 城只做 A

**工作分解**：
1. 选图：每城 4 张，共 48 张
2. URL 替换 `src/data/cities.ts` 的 `img()` 工厂
3. 给每张图配 `focus`（保留现有 focus 字段）
4. 验证：板块 2 / CityIndexPage / CityPage hero/related 都正确切换

**验收**：
- 12 城 × 4 时段 = 48 张图，**至少 36 张不重复**
- 同一城市 4 张图视觉上"早午晚夜"差异明显
- WebP / srcset 自动复用（PR #9 的 imageUrl 工具已经支持）

### PR #12 · 字段名 `cn` → `zh` 全局化

**目标**：把 v1.2 PR #A 的局部重命名推广到全仓。

**改的字段**：
- `src/data/liveMoments.ts`：`contentTypeCn` / `categoryLabelCn` → `Zh`
- `src/data/moments.ts`：`cityCn` / `countryCn` / `categoryLabelCn` / `textCn` → `Zh`
- `src/components/EventDrawer.tsx`：`event.contentTypeCn` → `event.contentTypeZh`
- `src/components/MomentsTimeline.tsx`：`ev.contentTypeCn` → `ev.contentTypeZh`
- `src/components/EventWeatherChip.tsx` 等其它引用点

**预期影响**：约 50 处重命名，分布在 4-6 个文件。0.5 天可完成。

**验收**：
- `grep -rnE "\b\w+Cn\b" src/ | grep -v ".module.css"` 输出空
- typecheck 通过

---

## Phase 2 · 体验增强（v1.3 P1）

### PR #13 · 全局键盘快捷键

**目标**：让 power user 不用鼠标也能完整使用产品。

**快捷键映射**：
- `←` / `→` — 翻城市（板块 2 已有）
- `j` / `k` — 上下移动列表焦点
- `/` — 聚焦 search box
- `?` — 弹出快捷键帮助
- `Esc` — 关闭抽屉 / 退出当前态
- `g h` — go home
- `g c` — go /cities
- `g a` — go /about

**实现**：
- 新增 `src/hooks/useHotkeys.ts`（0 依赖，纯 keydown listener）
- 在 `AppRoutes` 顶层挂载
- 快捷键帮助做成 `src/components/HotkeyHelp.tsx`，`?` 触发

**验收**：
- 5 个核心快捷键可用
- 在 input 聚焦时不触发（避免与搜索冲突）
- 帮助面板可关闭

### PR #14 · PWA + 离线缓存

**目标**：让用户可以"装到桌面"、"断网也能看已访问过的城市"。

**实现**：
- 新增 `public/manifest.webmanifest`（PWA manifest）
- 新增 `public/sw.js`（service worker，Workbox 0 依赖手写）
- 缓存策略：app shell + 已访问城市图片（stale-while-revalidate）
- 在 `index.html` 加 `<link rel="manifest">` + 注册 SW

**验收**：
- Chrome → 三个点 → "安装 See Earth" 可装
- 断网后访问已访问过的城市能正常显示
- Lighthouse PWA 检查通过

### PR #15 · Lighthouse 性能门禁

**目标**：在 CI 跑 Lighthouse 性能审计，< 90 分就 fail。

**实现**：
- 新增 `.github/workflows/lighthouse.yml`（GitHub Actions）或 `scripts/lighthouse-ci.sh`（本地）
- 跑 `lighthouse` CLI 3 次取 median
- 指标：Performance / Accessibility / Best Practices / SEO 都 ≥ 90
- 输出报告到 `lighthouse-report.html`

**验收**：
- 当前线上版本 Performance ≥ 95（PR #9 已经把图片优化了）
- CI 跑通

---

## Phase 3 · 延后到 v2.0 的项目

> 这些**不在 v1.3 范围**，列出来是因为它们常被想起：

| 项 | 原因 |
|---|---|
| DAILY DIGEST（邮件订阅） | 需要后端 / 邮件服务，1-2 周工作量 |
| 多语言 / 英文版 i18n | 90% 受众中文用户，翻译工作量大 |
| "我的关注城市" / 收藏 | 与 v1.2 PR #4 "不个性化" 原则冲突 |
| 评论 / 社区 | 需后端 + 内容审核，3 周+ |
| 用户账号系统 | 需后端 + 隐私合规 |

---

## 实施计划

```text
Week 1: PR #11 (多时段图) + PR #12 (字段 zh 化)
Week 2: PR #13 (键盘) + PR #14 (PWA)
Week 3: PR #15 (Lighthouse CI) + 测试 + v1.3 部署
```

---

## 风险与决策点

- **多时段图来源**：方案 A (Unsplash) 最快但可能撞图；方案 B (自拍) 最有品牌感但慢；**建议先用 A，v2.0 再补 B**
- **PWA 与 SEO**：SPA 的 PWA 会被 Google 优先收录，但需要 Service Worker 正确处理 fetch。**注意 robots.txt 不要 block /sw.js**
- **Lighthouse 门禁的噪音**：性能分数会因为 Vercel 边缘节点的波动 ±3 分，**建议阈值 90 而不是 95**
- **v1.3 → v1.4 候选**：多语言 / 个性化（如果品牌立场调整）/ 移动端原生 App
