# 看见地球 v1.2 开发路线图

> 从 v1.1 "可运行" 推进到 v1.2 "内容完整、可被搜索、可分享"。

## 目标

- **功能层**：补齐板块2 的12 城详情页，让点击有内容
- **分发层**：让每座城市可以被 Google索引
- **体验层**：让图片加载更快、分享链接更专业

## 优先级总览

| 优先级 | 任务 | 工作量 | 价值 |
|---|---|---|---|
| P0 | /cities 列表页 | 1 天 | 高 |
| P0 | /cities/[slug] 详情页 | 1.5 天 | 高 |
| P1 | SEO meta + OG + sitemap | 1 天 | 中 |
| P1 | 图片 WebP + srcset | 1 天 | 中 |
| P2 | About / Editorial Method 页 | 0.5 天 | 中 |

**总计**：约 5–6 天可达到 v1.2 上线门槛。

---

## Phase 1 · 内容完整化（v1.2 核心）

### PR #5 · /cities 列表页

**目标**：让用户从板块2 底部 `SEE ALL 12 CITIES →` 进入，看到完整城市目录。

**页面结构**：

```
顶部导航
章节标题
  ATLAS · 12 CITIES
  A complete map of cities, observed one at a time.
城市网格 (4 列 × 3 行)
  每个城市:
    - 编号 01–12
    - 中文 + 英文名
    - 国家
    - 缩略图
    - hover: 显示当前时间 + 一句观察
底部: 返回首页
```

**实现要点**：

- 使用现有的 `src/lib/cities.ts` 数据
- 每张缩略图使用 `image` 字段
- 路由: `src/pages/Cities.tsx`
- 复用 `CityCard` 组件
- 移动端：2 列
- 平板：3 列
- 桌面：4 列

**验收**：

- /cities 页面渲染 12 个城市
- 每个城市点击跳转到 `/cities/[slug]`
- 移动端不出现横向滚动
- SPA 路由刷新不报 404

### PR #6 · /cities/[slug] 详情页

**目标**：让"VIEW CITY →"跳转有完整内容。

**页面结构**：

```
顶部导航
城市主图 (与板块2 主图一致)
城市标题 (中文 + 英文)
国家当地时间 + 时区
"城市此刻" 组件:
  - 当前 UTC 时间
  - 当地天气
  - 日出日落
  - 一句编辑观察
城市简介 (1-2 段)
编辑观察 (2-3 段):
  - 当地生活节奏
  - 季节性活动
  - 文化背景
相关城市 (3 个推荐):
  - 同大洲 + 同时区
返回上一页 / 返回首页
```

**实现要点**：

- 使用现有的 `getWeather()`、`getSunTimes()`、`getLocalTime()` API
- 路由: `src/pages/CityDetail.tsx`
- 数据: 从 `src/lib/cities.ts` 扩展每个城市的 `description` / `observations` 字段
- 移动端: 上下结构
- SEO meta 同步生成

**验收**：

- /cities/kyoto 渲染完整内容
- /cities/shanghai 渲染完整内容
- 时间、天气、观察基于真实 API
- 移动端可读可点击
- SPA 路由正常

### PR #7 · 内容数据补全

**目标**：补齐 12 座城市的简介、观察、文化背景。

**数据结构**：

```typescript
type City = {
  id: string;
  slug: string;
  nameZh: string;
  nameEn: string;
  countryZh: string;
  countryEn: string;
  latitude: number;
  longitude: number;
  timezone: string;
  image: string;
  images?: CityImage[]; // 多时段图片
  href: string;
  description: string;        // 城市简介 (200-400 字)
  observations?: string[];    // 编辑观察 (3-5 条)
  cultureBackground?: string; // 文化背景 (100-200 字)
  cityNow?: {
    oneObservation: string;
  };
};
```

**12 城内容生产清单**：

| 城市 | 简介 | 观察 | 文化背景 |
|---|---|---|---|
| 京都 | ✓ | 5 条 | ✓ |
| 里斯本 | ✓ | 5 条 | ✓ |
| 上海 | ✓ | 5 条 | ✓ |
| 墨西哥城 | ✓ | 5 条 | ✓ |
| 东京 | ✓ | 5 条 | ✓ |
| 里约 | ✓ | 5 条 | ✓ |
| 雷克雅未克 | ✓ | 5 条 | ✓ |
| 开普敦 | ✓ | 5 条 | ✓ |
| 伦敦 | ✓ | 5 条 | ✓ |
| 柏林 | ✓ | 5 条 | ✓ |
| 罗马 | ✓ | 5 条 | ✓ |
| 悉尼 | ✓ | 5 条 | ✓ |

**验收**：

- 12 城数据完整
- 简介 ≤ 400 字
- 观察每条 ≤ 100 字
- 文化背景 ≤ 200 字

---

## Phase 2 · SEO 与分发（v1.2 增长）

### PR #8 · SEO meta + OG + sitemap

**目标**：让每座城市独立可索引、可分享。

**实现**：

- 每页 `<title>` / `<meta description>` 独立
- Open Graph:
  - og:title
  - og:description
  - og:image (城市主图)
  - og:url
- Twitter Card: summary_large_image
- sitemap.xml (自动生成)
- robots.txt (允许索引)
- canonical URL

**实现方式**：

- 静态生成 (build time): 使用 `vite-plugin-sitemap` 或手写
- 动态生成 (runtime): Edge Function

**验收**：

- 每页有独立 title / description
- 分享链接有 OG preview
- sitemap.xml 含所有 URL
- Google Search Console 可提交

---

## Phase 3 · 性能优化（v1.2 体验）

### PR #9 · 图片 WebP + srcset

**目标**：首屏 LCP < 2 秒。

**实现**：

- 所有图片转 WebP (节省 50%+)
- 主图从 1600x1600 降到 1200x800
- 缩略图 600x400
- 列表项小图 200x200
- 使用 `<picture>` + srcset:
  ```html
  <picture>
    <source srcset="kyoto-1200.webp 1200w, kyoto-600.webp 600w" type="image/webp">
    <img src="kyoto-1200.jpg" loading="lazy">
  </picture>
  ```
- 移动端不下载大图

**验收**：

- Lighthouse Performance ≥ 95
- LCP < 2 秒
- 首屏图片 < 200KB

---

## Phase 4 · 品牌补充（v1.2 加分）

### PR #10 · About / Editorial Method 页

**目标**：让用户知道产品是什么、为什么可信。

**页面结构**：

```
/about
- 产品介绍
- 编辑方法 (Editorial Method)
- 数据来源
- 不个性化说明
- 团队 / 联系方式
```

**验收**：

- /about 页面可访问
- 内容完整
- 与品牌定位一致

---

## 实施计划

```text
Week 1: PR #5 (列表) + PR #6 (详情)
Week 2: PR #7 (内容生产) + PR #8 (SEO)
Week 3: PR #9 (图片优化) + PR #10 (About)
Week 4: 测试 + v1.2 部署
```

---

## v1.2 → v1.3 路线图（next）

- 用户留存机制 (DAILY DIGEST)
- 多时段图片切换 (PR #6 升级)
- 交互优化 (键盘快捷键)
- PWA + 离线支持

---

## 风险与决策点

- **多时段图片**：v1.2 是否需要？如需要，PR #6 工作量 +1 天
- **个性化**：v1.2 是否引入"我的关注城市"？建议延后到 v2.0
- **多语言**：v1.2 是否做英文版？建议延后到 v2.0
