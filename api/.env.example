# SEE EARTH V1 · alpha-api · Phase 2 environment variables template
# ------------------------------------------------------------------
# Copy this file to `.env.local` for local development.
# DO NOT commit `.env.local` (already gitignored).
#
# Vercel production env vars (configure on `sethearth-2` project):
#   https://vercel.com/seethearth/sethearth-2/settings/environment-variables
#
# Reference:
#   - api/README.md (Vercel deployment notes)
#   - 06-PM Agent 交接/2026-08-25-pm-takeover-postmortem.md (Iron Rule 2: cross-verify)

# === Supabase (Phase 1 alpha project) ===
SUPABASE_URL=https://pyabuenednjbwshfayaa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...REPLACE_ME...
DIRECT_URL=postgresql://postgres:REPLACE_ME@db.pyabuenednjbwshfayaa.supabase.co:5432/postgres

# === Sentry (server-side, mirror of web client) ===
# Same DSN as web frontend (no VITE_ prefix here)
# Omit to disable Sentry initialization (Phase 1 contract: no-op without DSN)
SENTRY_DSN=https://4a38857119f70abba3a5dd23b9cf721e@o4511965043032064.ingest.us.sentry.io/4511965949394944

# === Node runtime ===
NODE_ENV=development
# VERCEL_ENV=development  # auto-set by Vercel in deployed environments
# VERCEL_GIT_COMMIT_SHA=  # auto-set by Vercel in deployed environments