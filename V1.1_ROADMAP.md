# 看见地球 v1.1 上线路线图

> 把项目从 v2.60.0 baseline（视觉 + 城市数据 + 天气 + 时区全完成）
> 推进到 v1.1 可上线版本：补齐 sunrise-sunset 真实时段 + SPA 部署兜底。

## 当前状态（2026-08-08）

- **基线版本**：v2.60.0（代码注释）
- **分支**：`codex/v1.1-real-data`（从 `codex/m0-scaffold` 拉出）
- **基线 commit**：`214f47e chore: baseline v2.60.0 before v1.1 real-data sprint`
- **Git 拓扑**：本地只有 M0 scaffold + 本次 baseline 两个 commit；**无远程仓库**

## 计划 vs 实际

| 项 | PM 计划假设 | 实际状态 |
|---|---|---|
| PR #1 open-meteo | 待写 `lib/weather.ts` | ✅ 已存在 `src/lib/weather.ts`（15min 缓存 + in-flight 去重 + 兜底） |
| PR #3 timezone via Intl | 待写 `lib/timezone.ts` | ✅ 已存在（`src/data/cities.ts` 内 `getCityNow` / `getCurrentPeriod` / `getTimezoneAbbrev`，全用 `Intl.DateTimeFormat`） |
| PR #2 sunrise-sunset | 待写 `lib/sun.ts` | ❌ 仍按小时桶分时段 |
| PR #4 serve -s dist | 待改 `package.json` | ❌ `serve` 未安装，无 `start` 脚本 |

→ 实际只剩 PR #2 + PR #4 需要新写代码；PR #1 / #3 通过 baseline commit 一次性纳入。

## 阻塞项（P0）

### PR #2 · feat(sun): integrate sunrise-sunset

**范围**

- 新增 `src/lib/sun.ts`：调用 `api.sunrise-sunset.org/json`
- 按 (citySlug + dateKey) 缓存（一天内复用）
- 失败兜底：回到现有 `getCurrentPeriod` 的小时桶逻辑
- 暴露 `getSunPeriod(sun, now)` → `DayPeriod`，替代纯小时桶
- CityNow 调用 getSunPeriod 决定 dayPeriod；图片选择走 `pickImage`

**验收**

- 12 座城市日出日落真实（URL 含 lat / lng / date / formatted=0）
- 多时段图片按真实 dawn / morning / afternoon / evening / night / deepNight 切换
- API 失败 → 自动降级到小时桶，控制台一行 warning

### PR #4 · chore(deploy): add serve -s dist with SPA fallback

**范围**

- 新增 `serve` 依赖
- `package.json` 新增 `"start": "serve -s dist -l 8080"`
- 新增 `.env.example` / `.env.production` 占位（为后续 WEATHER_API_PROVIDER 预留）
- `vite.config.ts` dev server 已绑 `0.0.0.0:5173`，preview/start 用 serve 8080

**验收**

- `npm run build` 成功
- `npm run start` 后台启动
- `curl -I http://localhost:8080/cities/kyoto` → 200
- `curl -I http://localhost:8080/cities/shanghai` → 200
- `curl -I http://localhost:8080/cities/lisbon` → 200
- 浏览器刷新 `/cities/kyoto` 不再 404（SPA fallback 生效）

## 后续 PR（v1.1 → v1.2）

### PR #5 · feat(cities): /cities list & detail pages

- ✅ `CityIndexPage` + `CityPage` 已存在
- 待补：12 城完整简介 + 编辑观察 + 每城 3–6 张多时段图（已部分就绪）
- 板块 2 hover 同步 + 跳转真实可达

### PR #6 · chore(seo): per-page meta + OG

- 每页独立 title / description
- OG / Twitter Card
- sitemap.xml / robots.txt
- canonical URL

### PR #7 · chore(perf): webp + srcset

- 图片转 WebP / AVIF
- 主图 1200×800
- srcset / sizes
- Lighthouse Performance ≥ 95

## 时间表

```text
Week 1: PR #2（sun）+ PR #4（deploy）        ← 本次 sprint
Week 2: PR #5 内容补齐 + 上线 smoke
Week 3: PR #6 + PR #7
Week 4: 测试 + 上线
```

## 当前进度

- [x] 基线 commit（v2.60.0 全部纳入）
- [x] V1.1_ROADMAP.md
- [ ] PR #2 sunrise-sunset
- [ ] PR #4 serve -s dist + .env
- [ ] PR #5 cities
- [ ] PR #6 seo
- [ ] PR #7 perf

## 已知约束

- **无远程仓库**：本次 sprint 不 `git push`，不创建 GitHub PR
- **typecheck**：单跑 `tsc -b --noEmit` 会报 `tsconfig.node.json may not disable emit`，
  原因是 composite 项目的 noEmit 冲突；`npm run build` 里走的是 `tsc -b`，一切正常
- **图片来源**：当前城市图统一用 Unsplash（带 `imageCredit`），
  后续如需本地图床或 CDN，按 PR #7 处理
