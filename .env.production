# See Earth · production environment
# Consumed by Vite at build time (variables must be prefixed VITE_ to be
# exposed to client; bare keys are only visible in build tooling / serve).

WEATHER_API_PROVIDER=open-meteo
SUN_PROVIDER=sunrise-sunset
